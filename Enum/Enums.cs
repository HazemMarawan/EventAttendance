using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace EventAttendance.Enum
{
    public enum Gender
    {
        Male = 1,
        Female = 2,
    }
    public enum UserType
    {
        Staff = 1,
        Member = 2,
    }
}